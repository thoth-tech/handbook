{
  "name": "handbook",
  "description": "Thoth Tech company handbook",
  "private": true,
  "scripts": {
    "format": "prettier --write \"**/*.md\"",
    "format:check": "prettier --check \"**/*.md\"",
    "prose:check": "vale --minAlertLevel error docs",
    "lint": "npm run format:check; npm run prose:check"
  },
  "devDependencies": {
    "prettier": "^2.6.2"
  },
  "engines": {
    "node": ">=16.14.2",
    "npm": "^8.5.0"
  }
}
